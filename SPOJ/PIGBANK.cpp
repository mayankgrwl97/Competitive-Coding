#include <bits/stdc++.h>
using namespace std;

int p[500];
int w[500];

int dp[500][10001];

int func(int wt, int n)
{
	if(wt<0)
		return 0;
	
	

	return;
}

int main()
{
	#ifndef ONLINE_JUDGE
		freopen("input.txt","r",stdin);
		// freopen("output.txt","w",stdout);
	#endif

	
	int t;
	scanf("%d",&t);
	
	while(t--)
	{
		cin>>e>>f;
		cin>>n;
		for(int i=0; i<n; i++)
		{
			cin>>p[i]>>w[i];
		}
		func(f-e, n);
	}
	
	
	return 0;
}